# Validation

- `node scripts/twilight/validate.mjs`: passed. 33 badges, 20 host offsets, 5 movie completions, 4 meals, exact dependencies for all 4 bonuses, 36 audio files, 33 finished illustrations, uniform Gowalla arches, and the dark default theme. Content version 8 refreshes cached assets.
- `node scripts/twilight/audio-cache.test.mjs`: passed. Event switching and content-version changes refresh narration; late old preloads cannot overwrite current audio.
- `npm run test:e2e`: passed. Build, onboarding, all 33 images loading, honor-system claiming, Forever unlock, two consecutive silent app updates, live content update, Shire regression, and subdomain mode. Its programmatic clicks triggered expected browser autoplay warnings; the real-input check below covers actual playback.
- `node scripts/twilight/content-smoke.mjs`: passed with trusted browser mouse input. Dark mode applies even with light browser preferences and before React loads; the initial body color is RGB(15, 28, 34). All 33 cards use arches. Scene narration and bonus narration played successfully; Twihard and the meal bonus unlocked independently; the final movie triggered Forever and Immortal. After final team confirmation, the actual downloaded team poster was 1080 × 2218 px and included the full character portrait and all 33 badge images in arches. The dark app and export were visually inspected. The earlier generic certificate remains available for partial collections.
- `node scripts/twilight/motion-smoke.mjs`: passed. Badge imagery appeared in 108 ms versus the earlier 952 ms measurement, with 5 layout operations versus 35 in the local test browser. Tests cover stable swipe height, canceled claims when changing badges, complete exits, scroll and focus restoration, Escape/Tab handling, reduced motion, queued bonuses and double clicks, and onboarding recovery when media never finishes loading. These are local measurements, not a device performance guarantee. Camera hardware was not tested.
- All 36 Eleven v3 dialogue MP3s decoded without errors. Nine distinct character voices were designed with `eleven_ttv_v3`; the full character descriptions were submitted to Voice Design, and each clip's emotion tags appear in its actual TTS request. Receipts match voice IDs, design hashes, exact submitted text, settings and audio hashes. Scribe v2 verified every spoken word after punctuation, case and space normalization; two wording discrepancies were resolved before publication. Clips span 1.15–4.36 seconds, totaling 85.92 seconds, at natural speed 1.0. See `audio-validation.json`. This verifies content, request wiring and decoding; it does not claim human listening approval. The initial three performances were shared for feedback, and the full nine-character sampler is available.
- All 33 generated illustrations were inspected, including the five approved samples, the complete contact sheet, and in-app crops. A small generated signature on the meadow illustration was removed with the image model and the clean result inspected. Originals and generation receipts are retained; the complete share ZIP includes the filterable gallery and optimized WebPs.
- `git diff --check`: passed. Repository text scan found no API key written into source, public content, scripts, or documentation.

Local development preview: `http://127.0.0.1:5175/event/twilight` while Vite is running. Production is published at [twilight.checkins.party](https://twilight.checkins.party); see `DEPLOYMENT.md` for the live release checks. See `CONTENT-NOTES.md` for scene cues that need a check against the actual playback editions.

- `node scripts/twilight/team-smoke.mjs`: passed. Required initial choice, legacy progress preservation, three-way carousel wrapping in both directions, real touch swipe, 320px mobile and 1280px desktop layouts, reduced motion, first/final persistence, finale sequencing, confirm/change paths, no repeated poll after reload, reset, and Shire isolation. All three actual PNG downloads contain 33 arches and one portrait without cropping. All three team exports and mobile carousel views were visually inspected. Web Share calls and cancellation were exercised with a test double; an actual device's native share sheet was not opened.
- All three 768 × 1152 portrait WebPs have alpha transparency. Charlie was revised with the built-in image tool from the host’s Billy Burke references: leaner frame, tired eyes, loose overshirt and a beer. The generated checkerboard was removed when preparing the alpha cutout. His updated carousel and full poster were visually inspected, and `team-smoke.mjs` passed again. Original PNGs, references, packaging notes and exact prompts are saved in `team-art/`.

- Copy revision: all 33 descriptions are at least 50% shorter, averaging 23.1 words versus LotR’s 21.7. Snippets average 3.1 words; the introduction is 33 words. `validate.mjs`, the production build, and `content-smoke.mjs` passed. Mobile scene, meal and team views were visually inspected at 390 × 844; the sampled claim buttons fit without scrolling or horizontal overflow. See `COPY-LENGTHS.md`.
